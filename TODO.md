# TODOs

- [ ] Improved idempotency checks for each step
- [ ] Show a list of steps "Docker" style, with a small summary of what's executing under each step line.
- [ ] --force mode to skip idempotency checks
- [ ] Improved keybinding patterns

Is it possible to always prefer a security key over Safari passkey and Bitwarden?

Tmux Crap:
- I open a session in dotfiles, cd into a project, and all new panes are made under dotfiles, BAD!
- I want to open a new session in a folder that an existing session is connected to, not open the existing session!
- Auto-name sessions based on directory name + worktree
- I closed a Ghostty window, but the tmux session seems to have been zombied. I can see the session in the session list, but I get a bad exit code when I try to open the session. There's also a dotnet process still running from that session.

Claude Crap:
- I want my Claude chats to survive across worktrees
